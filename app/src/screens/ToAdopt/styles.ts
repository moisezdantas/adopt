import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    typeAnimal: {
        marginTop: 5,
        padding: 20,
    },
    animal: {
        marginTop: 10,
        paddingLeft: 20,
        paddingHorizontal: 20,
    }
});
